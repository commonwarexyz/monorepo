# Multimmit properties and evidence

This document is for readers checking the implementation against the
[Multimmit specification](https://arxiv.org/abs/2607.21021v5) (arXiv 2607.21021, version 5). It lists
where the code departs from the paper, the properties the code must keep, the tests and fuzz
campaigns that exercise each property, and what that evidence leaves out.
[`STATE_MACHINE.md`](https://github.com/commonwarexyz/monorepo/blob/main/consensus/src/multimmit/docs/STATE_MACHINE.md)
describes the state, transitions, and invariants behind these properties, and its
[Terminology](https://github.com/commonwarexyz/monorepo/blob/main/consensus/src/multimmit/docs/STATE_MACHINE.md#terminology)
section defines the terms used here.

## Assumptions and evidence

Liveness assumes that at most `f` participants are faulty, crashed, or unavailable in total, that
messages between correct participants arrive after GST, that the runtime and storage keep working,
and that the application eventually takes custody of every block from a correct producer. Outside
those assumptions the implementation must stay bounded and fail closed, but it need not make
progress.

The evidence cited here comes in four kinds:

- **Tests** run a fixed, deterministic scenario or matrix through the production code. They catch
  regressions. They do not prove a protocol claim in general.
- **Fuzz campaigns** let input bytes choose protocol actions, topology, failures, or artifacts.
  Saved inputs reproduce failures and mutation widens the schedules covered, but a campaign is still
  not a proof.
- **Code inspection** covers claims about types, ownership, call graphs, or the absence of
  something: for example, that consensus keeps no dense delivery cursor, that Catalog is the only
  archive owner, or that certificates never reach `Automaton`.
- **Environment assumptions** are conditions that the model, the application, or the runtime must
  supply. A test can run a schedule that satisfies one, but it cannot show that the environment
  always will.

## Deviations from the specification

These are the places where the implementation departs from the paper or leaves out one of its
optional features. The [crosswalk](#specification-crosswalk) below maps every section, lemma, and
theorem.

| Specification item | Implementation | Consequence |
|---|---|---|
| Section 4.1, Algorithm 1, DA votes | A validator DA-votes for a block only after `Automaton::verify` returns `true` for it. The paper DA-votes every eligible block with no application check. | The paper's liveness proof assumes its own rule, so liveness here also assumes that every block from a correct producer eventually verifies at every correct validator (P-AUTO-1, P-LIVE-1). |
| Section 4.2, Algorithm 2, `ProposeChains` | The leader proposes each chain at its highest held DA certificate. Newer blocks reach voters as vote extensions, at most `e` per chain. | Proposal validity still admits up to `d` blocks per chain, so voters accept a leader that follows the paper (P-PIPE-1, P-VQC-1). |
| Section 4.2, pool settledness | Core uses `max_child_support + (n - \|P\|) <= f` in place of the paper's `beta + (n - \|P\|) <= f`. | The rule is more permissive when votes beyond the tip split across incompatible branches. [Leader finality](https://github.com/commonwarexyz/monorepo/blob/main/consensus/src/multimmit/docs/STATE_MACHINE.md#leader-finality) explains why it stays safe (P-TIPS-1). |
| Section 4.2, retained pool set `S` | An unfinished pool retires with its bounded view window. | A late vote from before GST can cost a local finality opportunity. Within the fault bound, a covering L-QC restores finality (P-RET-1, P-LEAD-1). |
| Section 6, backup proposals | Not implemented. | Timeout and nullification recover from a crashed leader. |
| Section 6, ordering by reputation | Not implemented. | Marshal keeps the fixed horizontal chain order, with no blame set or per-view shuffle. |
| Section 6, one signature scheme | Not implemented. | DA certificates and nullifications use threshold signatures. Votes and V-QC/L-QC transcripts use aggregate signatures. |
| Section 6, transport suggestions | Content-addressed vote transport and advertise-and-pull forwarding are not implemented. | Votes and certificates travel as complete messages. Batch verification, from the same section, is implemented. |
| Theorem 4, generalized threshold | Not implemented. | The extension threshold is `n - f`. |

## Properties

Each property names the component that owns it and the durable record that witnesses it. The
[evidence index](#evidence-index) lists the tests for each ID.

| ID | Property | Owner | Durable witness |
|---|---|---|---|
| P-ADM-1 | Hostile input, including bytes read back during recovery, cannot use unbounded memory, crash the process, poison a later valid artifact, or get a transport peer quarantined without objective evidence. Rejecting input grants no authority. | Ingress, verifier, Core admission, bounded storage readers | Only admitted events are recorded. Input enters through observation, verification completion, dependency resolution, or recovery decode. |
| P-OBS-1 | Sticky selections and transcripts follow the order in which artifacts were observed, whatever order the workers finish verifying them in. | Core admission, view, and finality | Observation assigns each artifact an identity, a verdict admits the artifact against that identity, and the selected transcript is recorded as an event. |
| P-OWNER-1 | Core is the only owner of durable protocol state and authority. The voter feeds Core typed inputs and runs the capabilities Core returns. Chain planes hold volatile block copies, application jobs, and derived eligibility, but only Core reserves a DA choice or advances a certified frontier. Producer inputs stay separate from view and leader inputs. | Voter, chain planes, `CoreState` and `Machine` | `Input` variants, generation-scoped `ValidatorCommand`s and `DaVotesOffer`s, and ordered `DomainEvent`s. |
| P-SIGN-1 | A validator signs at most one subject per protocol signing slot, across crashes. | Core durability and signing slots | The reservation event, made durable before the signature is exposed and replayed on recovery. |
| P-ACCT-1 | Two conflicting authenticated claims prove producer, leader, vote, vote/novote, or DA-vote equivocation. When both claims are held in bounded live evidence, Core quarantines the signer and never an honest relay that carried the evidence. Omission, timeouts, application rejection, and other suspicions that cannot be proven to others never trigger quarantine. | Protocol types and signature scheme, Core accountability index, voter blocking path | None. Evidence comes from verified artifacts or certificate transcripts and lives in a bounded volatile index. |
| P-EXP-1 | Nothing created locally that carries authority is exposed until the journal range covering it is durable. Each reducer poll emits at most one contiguous journal range and runs only when a journal command slot is free. | Core durability acknowledgement, journal owner | The staged cursor range, the single journal command slot, and the acknowledged cursor, with an explicit release outcome. |
| P-REP-1 | Recovering from a durable prefix gives the same state as running without interruption up to that prefix. | Core durability, snapshot and event reducer | A version-0 snapshot at the acknowledged cursor plus the contiguous journal suffix. |
| P-PUB-1 | A publication is persisted before it is sent, is idempotent across crashes, and is retired only by its semantic successor. Exit proofs for different views may become ready in any order, and their live obligations share the durable outbox limit instead of a separate per-view certificate count. | Core publication, Egress | A stable effect ID, a typed durable obligation, and a typed successor event. |
| P-BND-1 | Every queue, job, retained object, publication turn, and disk suffix has a validated finite limit. A derived mirror has the same limit as the state it mirrors and cannot reject work its owner already admitted. | Resource profile, Core service metadata and retention, collaborator permits | Service metadata is volatile. The retained set lives in snapshots and events. Retry deadlines pick a fixed fair quantum, and recovery compacts a due replay suffix before starting another generation. |
| P-AUTO-1 | Consensus handles only opaque application payload digests. A successful application check gates validity and availability for producer and DA authority. `Automaton` is never used to fetch or verify V-QCs, nullifications, L-QCs, or leader blocks. | Automaton executor, chain planes, Core producer chains and DA | The producer context, parent, and payload completion, a generation-scoped offer of eligible runs, and then a durable signing reservation. |
| P-CUST-1 | After recovery, authority that depends on producer payloads stays private until `Automaton::verify(context, payload_digest)` confirms again that the payload and its parent are available. Multimmit adds no application supertrait or storage protocol, and consensus never fetches payloads. | Voter recovery phase, Core producer reachability | A sorted, bounded set of payload requirements derived from retained reachability. Each must verify `true` before dependent work is released. |
| P-PIPE-1 | Producer and DA choices that carry authority form a contiguous run of at most `d` blocks above the certified anchor. Speculative verification may finish out of order. Producer ancestry uses canonical header identities, while `Automaton` sees the separate opaque body commitment. | Core producer chains and DA | The header chain, the custody result, the producer or DA reservation, and certificate advancement. |
| P-DA-1 | Correct validators DA-vote once per `(chain, height)` along a contiguous held path. Certificates use one canonical `n - 2f` transcript and advance monotonically. | Core DA, DA recovery task | DA choice, share, and certificate events. |
| P-TIPS-1 | `Tips(V-QC)` uses every designated vote in a V-QC of `n - f` to `n` messages: it drops `f` and carries branches with `2f + 1` support. Finalized tips drop `3f` and need full extension support. Pool settledness uses the branch-specific rule in `STATE_MACHINE.md`. | Tip algebra, Core finality | Pure tally algebra and the finalized floor event. |
| P-COMMIT-1 | Every non-genesis leader block carries the correct tip commitment, which authenticates the full safe-tip history. | Tip history type, Core view | The leader's signing reservation includes the commitment. |
| P-LEAD-1 | `n - f` votes from a retained pool finalize a leader. An L-QC is portable evidence and does not gate local finality. Finality only moves forward within the bounded lifetime of a pool. | Core finality | The frozen sticky pool, the finalized floor event, and an optional L-QC event. |
| P-CONS-1 | Leaders finalized at correct replicas form one authenticated chain of parent V-QCs, and repeated reports of the same finality evidence agree. Compatible finalized tips feed one prefix-compatible marshal ordering. Consensus keeps no dense log of its own. | Core view and finality, with the marshal Synchronizer at the activity boundary | The finality fact and parent V-QC identity, the authenticated L-QC and history chain, and the ordered frontier. |
| P-VIEW-1 | Vote, novote, and nullify choices exclude each other. A valid V-QC or nullification exits exactly one view. Nullifying after voting follows the non-support rule. A receiver accepts any valid earlier parent that comes with the complete nullification gap, even when it knows a higher V-QC. | Core view and durable stance slots | Stance and signing events, the authenticated exit event, and the retained proof. |
| P-VQC-1 | A V-QC accounts for `n - f` to `n` distinct participants in disjoint, complete, attributed categories, with at least `2f + 1` votes for one proposal. Only the first new V-QC for a view is forwarded on its own. A proposal carries its selected parent unless that same certificate was already forwarded. | Core view and publication, V-QC tally | The immutable aggregation transcript, the fact that a view's first V-QC was forwarded, and the proposal's parent identity. |
| P-RES-1 | The resolver fetches only authenticated view-exit evidence. Demand is level triggered, and a covering L-QC re-anchors agreement without producer history. Exits may retire at the bounded retention floor. Catching up assumes at most `f` unavailable or Byzantine participants. | Core resolution, `ResolverCommand` capabilities, resolver actor | A retained proof or the finality floor satisfies demand. Retry state is volatile. |
| P-ASYNC-1 | Only a completion that matches a live request can change state or capacity. Completion order affects latency only: future exit proofs and producer-chain work may finish out of view or height order, and recovery keeps each live obligation without adding a second capacity contract. | Core capability identities, collaborator permits | Generation, job, request, subject, and transcript identities. The resulting event is the witness. |
| P-SNAP-1 | Only Core takes snapshots, and only of acknowledged state. Pruning never removes the only durable copy of a later event, and continuous load that produces authority cannot starve the snapshot cut. | Core `Snapshot`, journal owner | A checkpoint fence drains the staged prefix, the cut is taken at the acknowledged cursor, and every record above the cut is kept. |
| P-LIVE-1 | A stalled or faulty producer delays only its own chain. View and leader progress and the other producer chains stay live, assuming every correct producer block eventually verifies `true` with durable custody at every correct validator. | Core per-chain worklists and discardable scheduler | No extra durable witness. Healthy chains keep advancing certificates, finality, and view exits. |
| P-RET-1 | One retirement pass bounds history without deleting a live dependency, proof, job, reservation, or publication. | Core retention | Reachability from the current floors and live tables. Snapshots contain the retained set. |
| P-BOUND-1 | Consensus exposes agreement facts only. It owns no bodies, no dense order or delivery state, and no application acknowledgement cursor. Reporter feedback carries no authority. | Engine API, Core snapshots, sparse inspection and activity | Their absence from consensus lifecycle types, durable codecs, snapshots, and normalized diagnostics. |
| P-MATTACH-1 | Consensus reports authenticated L-QCs, producer headers, and history openings it can rebuild locally as idempotent, best-effort `Activity`. Reporter feedback never gates progress, creates a durable event, or retains an artifact. Marshal checks identities, batches useful hints into Catalog, and repairs losses through resolver backfill and startup reconciliation. | Core activity drain, voter Reporter executor, marshal Router and Synchronizer | Catalog admission in marshal, downstream of the accepted activity. |
| P-MSTORE-1 | Catalog is the only owner of mutable archives and checkpoints. Admissions by exact key are idempotent. A commit syncs changed archives before publishing a checkpoint that records any temporary cleanup still owed. Prunable finalized rows point at bodies already in custody, and application pruning reclaims both together. Immutable rows own complete blocks, and cleanup after a checkpoint reclaims every obsolete temporary candidate. Startup replays owed cleanup before serving reads, without another metadata sync. Installing a runtime floor first syncs one bounded intent holding the verified L-QC, opening, target checkpoint, and cleanup floors. That intent stays authoritative through archive installation and generation-bound pruning, and one ready state then publishes the target checkpoint and binds all three finalized backend modes across reopen. | Marshal Catalog and archive adapters | Finalized and temporary archive rows, plus the canonical ready, commit-cleanup, and install metadata in the catalog. |
| P-MRES-1 | Marshal fetches and serves only exact L-QC IDs, history commitments, and (producer chain, header digest) pairs. The compact producer wire key leaves out the height, but completing a request locally still needs the full `BlockRef`, and an out-of-range chain is rejected before any peer work. A fetch checks Catalog custody before asking peers. External bytes are size-bounded before decoding, identity-checked once, and authenticated before admission. Subscriptions never start network work. | Marshal resolver adapter and `commonware-resolver` | The per-subscriber request identity. Catalog admission is the durable witness. |
| P-MSYNC-1 | Authenticated recursive history is processed oldest first. Following the paper's proposal-first ordering variant, each sweep is offset-major in two passes: proposed blocks, then extensions. The final sweep stops at a positional shortfall in the first pass or at an unsettled chain in the second. Producer ancestry is fetched serially per chain and concurrently across chains under one global limit. Completion order cannot change delivery order. History openings and dense rows share bounded Catalog commits, and output indices stay contiguous. | Marshal Synchronizer, order and ancestry modules, Catalog commit | The selected L-QC, history index, ordered and emitted frontiers, per-chain ancestry cursors, contiguous output rows, and the committed high-water mark. |
| P-MDEL-1 | Reporter receives the complete transaction block at the next `OutputIndex`, and its `Digestible` identity is the consensus `BlockRef` digest. Several `Exact` acknowledgements may be in flight, but acknowledgement advances only over their contiguous resolved prefix, followed by one checkpoint sync. A crash may repeat that prefix and cannot skip it. Durable hot-body notifications are byte-bounded and advisory, so mailbox pressure or a restart falls back to dense finalized reads. | Marshal Delivery and Catalog acknowledgement | The committed and acknowledged checkpoint cursors. |
| P-MBOUND-1 | Marshal's working set does not grow with chain age or catch-up distance. Actor queues, jobs, subscribers, block waiters, history and output commit batches, hot delivery, and producer fetches are all bounded, and unbounded walks spill to linked segmented journals with only per-chain heads in memory. When later unique hints add pressure, missing-block waiters retire oldest first. Scratch is cleared after each transition and on reopen. Temporary L-QC and history rows and immutable block candidates are pruned after a commit or floor installation, while prunable finalized bodies stay in producer custody until application pruning reclaims them with their rows. How far final archives grow is a retention choice. If the finality or application-prune frontier stalls indefinitely, the required MultiArchive disk and its in-memory index can still grow. | Marshal Config, Router, resolver, scratch, Synchronizer, Delivery, Catalog | Validated capacities, producer concurrency and hot-byte limits, the temporary and application prune frontiers, and scratch heads. |

Marshal fetches authenticated history and complete transaction blocks, applies the deterministic
proposal-first ordering from Section 6 of the paper, runs the settled and unsettled sweeps,
reconciles exact duplicate blocks, and owns the durable delivery cursor. Application bodies stay
opaque, so duplicate transactions inside bodies are the application's concern. Consensus and marshal
meet only at the best-effort activity stream described by P-MATTACH-1.

## Evidence index

Each entry is the Rust path of a test function. `multimmit::` paths are relative to
`commonware-consensus`, and `commonware_consensus_fuzz::` paths are in the consensus fuzz crate.
Test-group macros may add a suffix to the name `libtest` reports. Focused tests exercise one
mechanism directly. Deterministic-runtime tests run real actors or whole clusters under the
deterministic runtime.

| ID | Focused tests | Deterministic-runtime tests |
|---|---|---|
| P-ADM-1 | `multimmit::wire::tests::exact_derived_frame_caps_reject_before_parsing`; `multimmit::actors::ingress::lanes::tests::replaying_faulty_peer_cannot_take_the_correct_peers_lane_share`; `multimmit::scheme::bls12381_threshold::tests::batch::artifact_batches_isolate_invalid_items_for_both_variants`; `multimmit::scheme::bls12381_threshold::tests::batch::batched_verdicts_match_for_every_artifact_kind_for_both_variants`; `multimmit::storage::journal::tests::oversized_record_is_rejected`; `multimmit::storage::journal::tests::recovery_rejects_complete_invalid_tail_record`; `multimmit::storage::snapshot::tests::checksum_mismatch_recovers_previous_snapshot` | `multimmit::actors::ingress::tests::rejects_malformed_and_wrong_epoch_traffic_without_blocking`; `multimmit::actors::ingress::tests::byzantine_replay_saturation_preserves_correct_peer_service`; `multimmit::actors::ingress::tests::a_da_vote_is_dropped_unless_its_sender_is_its_signer`; `multimmit::engine::tests::recovery_verification_precedes_actor_construction_and_dependent_effects` |
| P-OBS-1 | `multimmit::machine::tests::finality::completion_permutations_preserve_sticky_selection`; `multimmit::machine::tests::da::da_fork_selection_does_not_depend_on_verification_completion_order` | `multimmit::machine::testing::world::tests::honest_replicas_keep_compatible_finality_under_replayable_faults`; `multimmit::actors::voter::tests::recovery::attached_observer_matches_the_synchronous_core` |
| P-OWNER-1 | `multimmit::machine::core_state::tests::core_services_simultaneous_five_source_flood_with_exact_weights` | `multimmit::actors::voter::tests::recovery::attached_observer_matches_the_synchronous_core`; `multimmit::actors::voter::tests::tracing::ready_application_work_precedes_later_mailbox_traffic`; `multimmit::actors::voter::tests::admission::live_admission_preserves_core_local_priority_under_peer_flood` |
| P-SIGN-1 | `multimmit::machine::tests::view::timeout_choice_recovers_at_each_durable_crash_prefix` | `multimmit::tests::all_validators_recover_durable_timeout_batch_before_certificate` |
| P-ACCT-1 | `multimmit::scheme::bls12381_threshold::tests::signing::ordinary_signatures_are_role_and_domain_bound_for_both_variants`; `multimmit::machine::tests::crypto::objective_equivocation_is_attributed_for_min_pk`; `multimmit::machine::tests::crypto::objective_equivocation_is_attributed_for_min_sig`; `multimmit::machine::tests::proposal::proposal_equivocation_suppresses_direct_vote`; `multimmit::scheme::fuzz::tests::structured_artifact_campaign` | `multimmit::actors::voter::tests::admission::relayed_double_vote_blocks_its_signer_not_the_relays`; `multimmit::actors::voter::tests::admission::an_invalid_da_share_is_discarded_and_the_quorum_still_certifies`; `multimmit::actors::ingress::tests::a_relayed_da_vote_is_dropped_without_blocking_its_sender`; `multimmit::twins::scripted::twin_halves_equivocate_without_extra_weight`; `multimmit::twins::randomized::randomized_twins_correlates_blocks_with_observer_evidence` |
| P-EXP-1 | `multimmit::machine::tests::durability::barrier_acknowledgements_are_exact_and_ordered` | `multimmit::actors::voter::tests::durability::durability_cut_matrix_reissues_and_retires_exact_obligations` |
| P-REP-1 | `multimmit::machine::testing::driver::tests::runner_models_before_and_after_append_crash_cuts`; `multimmit::machine::testing::world::tests::honest_replicas_keep_compatible_finality_under_replayable_faults` | `multimmit::actors::voter::tests::recovery::attached_observer_matches_the_synchronous_core`; `multimmit::engine::tests::recovery_verification_precedes_actor_construction_and_dependent_effects`; `multimmit::tests::n7_f1_cluster_survives_crash_and_recovery`; `multimmit::actors::voter::tests::recovery::repeated_pre_ack_recovery_crashes_keep_the_suffix_bounded` |
| P-PUB-1 | `multimmit::machine::tests::durability::durable_broadcast_replays_with_stable_id`; `multimmit::machine::tests::view::vqc_forwarding_waits_for_the_earliest_observation_cohort`; `multimmit::machine::tests::durability::replay_accepts_reordered_future_exit_forwarding` | `multimmit::actors::voter::tests::discharge::successor_barrier_retains_installs_then_retires`; `multimmit::engine::tests::engine_restart_preserves_every_active_publication_family`; `multimmit::tests::duplicated_reordered_certificates_converge_after_healing` |
| P-BND-1 | `multimmit::machine::tests::admission::ingress_bounds_apply_before_verification`; `multimmit::machine::tests::view::memory_plateaus_while_finality_stalls`; `multimmit::machine::tests::producer::recovered_chain_signing_reservations_use_global_capacity`; `multimmit::machine::tests::config::extreme_accepted_artifact_ceiling_does_not_preallocate_scratch`; `multimmit::actors::voter::egress::tests::saturated_deadline_rotates_across_bounded_turns`; `multimmit::actors::ingress::lanes::tests::exercises_random_schedules` | `multimmit::actors::ingress::tests::stalled_ingress_worker_does_not_block_control`; `multimmit::tests::authenticated_flood_services_engine_control_and_every_da_chain`; `multimmit::tests::retained_artifacts_plateau_across_retention_windows`; `multimmit::tests::checkpoint_compaction_soak_survives_a_bounded_restart`; `multimmit::tests::seed_swept_jittered_cluster_stays_live`; `multimmit::actors::voter::tests::recovery::repeated_pre_ack_recovery_crashes_keep_the_suffix_bounded` |
| P-AUTO-1 | `multimmit::machine::tests::da::application_digest_collision_retains_distinct_header_ancestry`; `multimmit::machine::tests::crypto::vqc_accepts_a_reused_application_digest_on_distinct_ancestry`; `multimmit::machine::tests::crypto::lqc_accepts_a_reused_application_digest_on_distinct_ancestry` | `multimmit::actors::voter::tests::admission::application_eventual_validity_restores_correct_chain_liveness` |
| P-CUST-1 | `multimmit::machine::tests::producer::recovered_payloads_are_the_exact_local_producer_and_da_union`; `multimmit::storage::recovery::tests::recovered_payload_verification_fails_closed` | `multimmit::engine::tests::recovery_verification_precedes_actor_construction_and_dependent_effects`; `commonware_consensus_fuzz::multimmit_engine::tests::seeded_repeated_custody_recovery` |
| P-PIPE-1 | `multimmit::machine::eligibility::tests::a_held_run_is_offered_in_height_order`; `multimmit::machine::tests::producer::recovery_reissues_the_exact_durable_producer_choice` | `multimmit::tests::stalled_producer_does_not_block_other_chains`; `multimmit::tests::validator_recovers_durable_state_within_retention_window` |
| P-DA-1 | `multimmit::machine::tests::da::producer_recovers_the_canonical_da_quorum_and_retains_the_certificate` | `multimmit::tests::finality_keeps_pace_with_production`; `multimmit::tests::n7_f1_cluster_survives_crash_and_recovery` |
| P-TIPS-1 | `multimmit::algebra::tests::safe_tips_use_every_designated_vote_in_a_larger_vqc`; `multimmit::algebra::tests::final_tips_match_materialized_reference`; `multimmit::algebra::tests::cross_certificate_safe_and_final_tips_match_reference`; `multimmit::algebra::tests::cross_certificate_safe_tips_match_reference_across_designated_tallies`; `multimmit::algebra::fuzz::tests::attributed_histories_match_reference` | `multimmit::twins::scripted::twin_halves_equivocate_without_extra_weight`; `multimmit::twins::randomized::randomized_twins_smoke` |
| P-COMMIT-1 | `multimmit::types::history::tests::commitment_is_domain_separated`; `multimmit::machine::tests::proposal::proposal_accepts_exact_tip_history_and_rejects_an_incorrect_commitment` | `multimmit::tests::validator_beyond_retention_resumes_from_covering_lqc_before_new_epoch_bootstrap` |
| P-LEAD-1 | `multimmit::machine::tests::finality::pool_finality_precedes_lqc_assembly_and_grows_monotonically` | `multimmit::tests::finality_keeps_pace_with_production`; `multimmit::twins::scripted::twin_halves_equivocate_without_extra_weight`; `multimmit::twins::scripted::well_disseminated_block_survives_byzantine_leader_censorship` |
| P-CONS-1 | `multimmit::twins::randomized::reporter_oracle_rejects_conflicting_finality`; `multimmit::twins::randomized::reporter_oracle_rejects_cross_height_forks`; `multimmit::twins::randomized::reporter_oracle_rejects_cross_view_leader_forks`; `multimmit::twins::randomized::reporter_oracle_rejects_conflicting_exact_evidence`; `multimmit::marshal::protocol::fuzz::tests::ragged_history_replay_matches_explicit_order` | `multimmit::twins::randomized::randomized_twins_smoke`; `multimmit::marshal::tests::local_two_chain_delivery_is_offset_major_and_header_exact` |
| P-VIEW-1 | `multimmit::machine::view::slot::tests::slot_transition_table_is_exhaustive` | `multimmit::actors::voter::tests::resolver::skipped_view_nullification_resolves_from_a_peer_node`; `multimmit::tests::all_validators_recover_durable_timeout_batch_before_certificate`; `multimmit::tests::resolver_recovers_exact_view_proof_after_rearmed_restart`; `multimmit::actors::voter::tests::timeout::inactivity_timeout_requires_a_recent_quorum_of_transport_peers`; `multimmit::actors::voter::tests::timeout::inactivity_timeout_never_skips_the_local_leader` |
| P-VQC-1 | `multimmit::machine::tests::crypto::inbound_larger_vqc_decodes_verifies_admits_and_uses_every_vote_for_tips`; `multimmit::machine::tests::view::local_vqc_emits_quorum_then_grows_to_the_full_sticky_transcript`; `multimmit::machine::tests::proposal::proposal_accepts_exact_lower_parent_with_complete_gap`; `multimmit::machine::tests::durability::replay_requires_the_views_first_vqc_forwarding_before_a_current_lqc_floor`; `multimmit::actors::voter::egress::tests::previously_broadcast_parent_is_omitted_from_proposal`; `multimmit::actors::voter::egress::tests::updated_broadcast_parent_is_attached_to_proposal` | `multimmit::actors::voter::tests::recovery::attached_observer_matches_the_synchronous_core`; `multimmit::actors::voter::tests::recovery::updated_broadcast_parent_is_attached_to_a_live_proposal` |
| P-RES-1 | `multimmit::actors::resolver::tests::control_overflow_coalesces_only_between_job_barriers`; `multimmit::actors::resolver::tests::wrong_view_response_is_rejected_without_blocking_its_peer`; `multimmit::machine::tests::floors::future_lqc_advances_signing_and_proposal_floors_without_resolution_and_restores`; `multimmit::machine::tests::view::view_retention_retires_history_while_finality_stalls` | `multimmit::actors::resolver::tests::stalled_codec_worker_keeps_control_and_queries_live`; `multimmit::tests::resolver_recovers_exact_view_proof_after_rearmed_restart`; `multimmit::tests::validator_beyond_retention_resumes_from_covering_lqc_before_new_epoch_bootstrap` |
| P-ASYNC-1 | `multimmit::machine::tests::admission::mismatched_completion_does_not_consume_job`; `multimmit::machine::tests::durability::replay_accepts_reordered_future_exit_forwarding`; `multimmit::machine::tests::producer::recovered_chain_signing_reservations_use_global_capacity` | `multimmit::actors::verifier::tests::executes_machine_issued_jobs_with_exact_tickets`; `multimmit::actors::resolver::tests::stalled_codec_worker_keeps_control_and_queries_live`; `multimmit::tests::duplicated_reordered_certificates_converge_after_healing`; `multimmit::tests::seed_swept_jittered_cluster_stays_live` |
| P-SNAP-1 | `multimmit::machine::durability::codec::tests::snapshot_rejects_unsorted_maps`; `multimmit::machine::durability::codec::tests::snapshot_bounds_reservations_and_publications_together`; `multimmit::storage::journal::tests::rolled_sections_prune_behind_a_checkpoint`; `multimmit::storage::snapshot::tests::crash_during_replacement_recovers_old_then_completed_replacement_recovers_new` | `multimmit::actors::voter::tests::production::checkpoint_roll_and_prune_keep_control_live`; `multimmit::tests::checkpoint_compaction_soak_survives_a_bounded_restart` |
| P-LIVE-1 | `multimmit::machine::eligibility::tests::in_flight_validations_are_bounded_and_resume_on_release` | `multimmit::actors::voter::tests::admission::application_eventual_validity_restores_correct_chain_liveness`; `multimmit::tests::stalled_producer_does_not_block_other_chains`; `multimmit::twins::scripted::well_disseminated_block_survives_byzantine_leader_censorship` |
| P-RET-1 | `multimmit::machine::tests::view::memory_plateaus_while_finality_stalls`; `multimmit::machine::tests::producer::certified_chain_state_plateaus_without_ordering`; `multimmit::machine::tests::proposal::retired_leader_dependencies_plateau` | `multimmit::tests::retained_artifacts_plateau_across_retention_windows`; `multimmit::tests::stalled_producer_does_not_block_other_chains`; `multimmit::tests::checkpoint_compaction_soak_survives_a_bounded_restart` |
| P-BOUND-1 | `multimmit::types::block::tests::transaction_header_identity_binds_complete_ancestry`; `multimmit::machine::durability::codec::tests::snapshot_round_trips_every_state_field`; `multimmit::machine::tests::producer::recovered_payloads_are_the_exact_local_producer_and_da_union` | `multimmit::actors::voter::tests::relay::reporter_feedback_does_not_block_remote_block_admission` |
| P-MATTACH-1 | `multimmit::machine::tests::proposal::proposal_accepts_exact_tip_history_and_rejects_an_incorrect_commitment`; `multimmit::machine::tests::floors::parent_retirement_preserves_history_for_a_retained_leader` | `multimmit::actors::voter::tests::relay::reporter_feedback_does_not_block_remote_block_admission`; `multimmit::marshal::tests::finality_activity_resolves_a_missing_history_opening`; `multimmit::marshal::tests::production_engine_reporter_survives_engine_and_marshal_restart` |
| P-MSTORE-1 | `multimmit::marshal::storage::archive::tests::exact_identity_and_reopen_preserve_data`; `multimmit::marshal::storage::archive::tests::prune_depends_on_backend`; `multimmit::marshal::storage::blocks::tests::metadata_round_trip_authenticates_key_epoch_and_chain`; `multimmit::marshal::storage::archive::tests::put_once_keeps_multiplicity_and_ignores_repeated_identities`; `multimmit::marshal::actors::catalog::tests::recovery::immutable_commit_recovers_canonical_body_and_reclaims_losing_candidates`; `multimmit::marshal::actors::catalog::tests::admission::concurrent_admissions_share_one_cut_and_reject_mismatched_identities`; `multimmit::marshal::actors::catalog::tests::recovery::reopen_replays_cleanup_owed_by_published_commits`; `multimmit::marshal::actors::catalog::tests::install::floor_install_resets_delivery_then_prunes_the_old_generation`; `multimmit::marshal::storage::catalog::tests::install_crash_cuts_recover_the_installed_floor`; `multimmit::marshal::storage::catalog_state::tests::catalog_state_round_trips_every_recovery_obligation`; `multimmit::marshal::storage::catalog_state::tests::decode_rejects_adversarial_frontiers`; `multimmit::marshal::open::tests::reopen_rejects_changed_finalized_archive_modes` | `multimmit::marshal::tests::verified_floor_continues_and_current_generation_prunes_across_reopen` |
| P-MRES-1 | `multimmit::marshal::wire::tests::tags_are_canonical_and_invalid_tags_are_rejected`; `multimmit::marshal::actors::backfill::tests::local_custody_completes_before_peer_fetch`; `multimmit::marshal::actors::backfill::tests::exact_identity_and_ambiguity_are_distinct`; `multimmit::marshal::actors::backfill::tests::stale_subscriber_delivery_completes_live_waiters`; `multimmit::marshal::actors::backfill::tests::canceled_waiters_are_reclaimed_at_capacity`; `multimmit::marshal::actors::backfill::tests::deliver_enforces_the_exact_value_byte_bound`; `multimmit::marshal::actors::backfill::tests::deliver_rejects_malformed_and_wrong_identity_values`; `multimmit::marshal::protocol::ancestry::tests::rejects_wrong_coordinate` | `multimmit::marshal::tests::remote_resolver_backfills_exact_lqc_history_and_blocks`; `multimmit::marshal::tests::malformed_reports_do_not_stop_the_service` |
| P-MSYNC-1 | `multimmit::marshal::protocol::order::tests::horizontal_is_offset_major`; `multimmit::marshal::protocol::order::tests::final_sweep_reuses_authenticated_lqc_algebra`; `multimmit::marshal::protocol::order::tests::history_openings_are_oldest_first_and_frontier_is_exact`; `multimmit::marshal::actors::synchronizer::tests::walk::paper_order_is_offset_major_across_producers`; `multimmit::marshal::actors::synchronizer::tests::walk::adjacent_history_openings_share_bounded_commits`; `multimmit::marshal::actors::synchronizer::tests::publish::long_gap_uses_capped_dense_batches`; `multimmit::marshal::actors::synchronizer::tests::publish::completed_synchronization_retires_consumed_history_scratch`; `multimmit::marshal::actors::synchronizer::tests::publish::restart_reconciles_authenticated_duplicates_without_outputs`; `multimmit::marshal::actors::synchronizer::tests::floor::finalized_proofs_at_or_below_the_floor_are_idempotent`; `multimmit::marshal::actors::synchronizer::tests::floor::floor_installation_rejects_a_valid_frontier_rewind` | `multimmit::marshal::tests::local_two_chain_delivery_is_offset_major_and_header_exact`; `multimmit::marshal::tests::remote_resolver_backfills_exact_lqc_history_and_blocks`; `multimmit::marshal::tests::verified_floor_continues_and_current_generation_prunes_across_reopen` |
| P-MDEL-1 | `multimmit::marshal::actors::delivery::tests::restart_cursor_is_exactly_after_durable_acknowledgement`; `multimmit::marshal::actors::delivery::tests::overflow_coalesces_body_handoff_without_losing_committed_progress`; `multimmit::marshal::actors::delivery::tests::pending_acknowledgements_are_bounded_and_retire_fifo`; `multimmit::marshal::actors::delivery::tests::canceled_acknowledgement_stops_fifo_retirement`; `multimmit::marshal::actors::delivery::tests::delivery_cache_retains_the_earliest_byte_bounded_prefix` | `multimmit::marshal::tests::crash_redelivers_only_until_acknowledgement_is_durable`; `multimmit::marshal::tests::delivery_pressure_materializes_evicted_hot_blocks_from_custody`; `multimmit::marshal::tests::production_engine_reporter_survives_engine_and_marshal_restart` |
| P-MBOUND-1 | `multimmit::marshal::config::tests::validation_rejects_cross_context_configuration`; `multimmit::marshal::storage::scratch::tests::very_long_stack_is_oldest_first_without_sync`; `multimmit::marshal::storage::scratch::tests::reset_and_reopen_discard_every_prior_segment`; `multimmit::marshal::actors::synchronizer::tests::walk::paper_order_is_offset_major_across_producers`; `multimmit::marshal::actors::synchronizer::tests::walk::adjacent_history_openings_share_bounded_commits`; `multimmit::marshal::actors::synchronizer::tests::publish::long_gap_uses_capped_dense_batches`; `multimmit::marshal::actors::synchronizer::tests::publish::completed_synchronization_retires_consumed_history_scratch`; `multimmit::marshal::actors::delivery::tests::delivery_cache_retains_the_earliest_byte_bounded_prefix`; `multimmit::marshal::storage::archive::tests::put_once_keeps_multiplicity_and_ignores_repeated_identities` | `multimmit::marshal::tests::delivery_pressure_materializes_evicted_hot_blocks_from_custody`; `multimmit::marshal::tests::missing_block_pressure_retires_old_subscriptions`; `multimmit::marshal::tests::remote_resolver_backfills_exact_lqc_history_and_blocks`; `multimmit::marshal::tests::verified_floor_continues_and_current_generation_prunes_across_reopen` |

## Specification crosswalk

Each row maps a part of the paper to the properties that implement it. The status says whether the
mapping is direct, an optional feature from Section 6, a local refinement, or an assumption the
environment must supply.

| Specification item | Properties | Status |
|---|---|---|
| Model and cryptographic assumptions | P-ADM-1, P-SIGN-1, P-ASYNC-1 | Direct. Profiles require `n >= 5f + 1`, and verification binds the committee identity, the context, and the signed subject. Liveness also assumes at most `f` faulty or unavailable participants. |
| Section 4.1, transaction blocks and Algorithm 1 | P-AUTO-1, P-PIPE-1, P-DA-1, P-LIVE-1 | Direct, plus an application gate. Headers bind the chain, height, parent, and payload commitment. Core enforces one DA vote per height, contiguous ancestry, the depth-`d` window, and `n - 2f` certification. `Automaton::verify` adds a local validity and custody check before any authority. |
| Section 4.2, leader blocks, proposals, and Algorithm 2 | P-COMMIT-1, P-PIPE-1, P-SIGN-1, P-VIEW-1, P-VQC-1 | Direct, except for what the leader proposes. The scheduled leader anchors on a greatest-view V-QC and proposes each chain at its certified base, and the DA-voted blocks that the paper's `ProposeChains` would include travel as vote extensions instead. Validity still admits up to `d` blocks per chain, and receivers validate the referenced V-QC and the nullification gap. Vote, novote, and nullify choices occupy durable exclusive slots. |
| Section 4.2, V-QCs and L-QCs | P-VQC-1, P-LEAD-1 | Direct. A V-QC holds complete, disjoint, attributed view messages with at least `2f + 1` votes for its designated proposal. An L-QC or retained pool has at least `n - f` distinct votes. |
| Section 4.2, `Tips` and `Tips*` | P-DA-1, P-TIPS-1, P-LEAD-1 | Direct for the rank and carry thresholds. Safe tips use rank `f + 1` and support `2f + 1`. Finalized tips use rank `3f + 1` and support `n - f` above a fully supported proposal. |
| Section 4.2, pool settledness | P-TIPS-1, P-LEAD-1 | Local refinement. Core uses `max_child_support + (n - \|P\|) <= f` in place of the aggregate `beta + (n - \|P\|) <= f`. [Leader finality](https://github.com/commonwarexyz/monorepo/blob/main/consensus/src/multimmit/docs/STATE_MACHINE.md#leader-finality) gives the branch-specific argument, and differential algebra tests cover competing branches. |
| Section 4.2, `Ord` and `Emit` | P-CONS-1, P-MATTACH-1, P-MSYNC-1, P-MDEL-1 | Implemented by marshal. Consensus reports authenticated finality facts. Marshal resolves history and bodies, builds a deterministic prefix, persists it, and owns delivery acknowledgement. |
| Lemma 1, certified blocks are compatible | P-DA-1, P-PIPE-1 | Direct: one durable DA choice per chain and height, contiguous ancestry, exact certificate transcripts, and monotone certified anchors. |
| Lemmas 2-5: one vote per view, vote-novote exclusivity, designation, no nullification | P-SIGN-1, P-VIEW-1, P-VQC-1 | Direct: durable stance slots and exact quorum validation. Structured-artifact tests mutate the signer, category, context, and designation independently. |
| Lemma 6, the leader chain | P-CONS-1, P-LEAD-1, P-VIEW-1 | Direct: exact parent V-QC identity and proposal validation. Twins joins finality reports with digest-addressed wire evidence and requires complete, compatible ancestry for finalized leaders. |
| Lemma 7, safe extension | P-DA-1, P-TIPS-1, P-LEAD-1 | Direct for tip dominance. The local settledness rule needs its own argument, covered under pool settledness above. |
| Lemma 8, emission is a prefix | P-CONS-1, P-TIPS-1, P-MSYNC-1 | Direct for the chosen deterministic sweep. The algebra campaign compares incremental final tips with a materialized reference, and the marshal campaign compares incremental ordering and replay with an explicit enumeration. |
| Theorem 1, consistency | P-CONS-1, P-LEAD-1, P-MSYNC-1, P-MDEL-1 | End to end. Twins checks that finalized leaders and exact finality evidence are compatible, and marshal checks prefix-compatible ordering and durable delivery. These finite campaigns catch regressions and do not prove the theorem. |
| Lemmas 9-10 and Corollary 1, view synchronisation and exit | P-PUB-1, P-RES-1, P-VIEW-1, P-VQC-1, P-LIVE-1 | Direct under partial synchrony. Forwarding the first new V-QC or nullification, together with the resolver, supplies authenticated exit evidence. Timers and the non-support rule drive nullification, and exits advance one view at a time. |
| Lemma 11, correct-leader views | P-PIPE-1, P-PUB-1, P-VIEW-1, P-VQC-1, P-LEAD-1, P-LIVE-1 | Direct under partial synchrony. After a fair suffix, proposal construction, anchor dissemination, both voting routes, and pool finality follow the correct-leader path. |
| Theorem 2, liveness | P-AUTO-1, P-CUST-1, P-PIPE-1, P-DA-1, P-VIEW-1, P-LEAD-1, P-LIVE-1, P-MRES-1, P-MSYNC-1, P-MDEL-1 | Depends on the environment. It needs message delivery after GST, working runtime and storage, repeated views, eventual production and DA certification, application custody at correct validators, correct-leader finality, and retrieval from a correct holder. |
| Lemma 12, availability | P-AUTO-1, P-CUST-1, P-DA-1, P-TIPS-1, P-LEAD-1, P-MRES-1 | Direct, with a retrieval path added by the implementation. DA custody and certification establish holders, safe and final tip extraction decides which holder bound applies, and marshal retrieves authenticated blocks. Requiring every correct validator to verify `true` before its own DA vote is a stronger local gate than the lemma needs. |
| Lemma 13, membership finality | P-DA-1, P-TIPS-1, P-LEAD-1 | Direct threshold mapping. The guarantee follows from signed vote evidence, and consensus exposes no separate cursor for it. |
| Theorem 3 and Corollary 2, block inclusion and in-view finality | P-DA-1, P-TIPS-1, P-LEAD-1, P-LIVE-1, P-MSYNC-1 | Holds under the theorem's DA-vote and extension-depth hypotheses. The censorship scenario supplies them, and marshal realizes eventual placement. |
| Corollary 3, chain-local finality | P-CONS-1, P-TIPS-1, P-LEAD-1, P-BOUND-1, P-MATTACH-1, P-MSYNC-1 | Leader-chain compatibility and safe extension keep the finalized chain prefix across later views. Consensus authenticates that history, marshal resolves and orders it, and consensus exposes no dense delivery cursor. |
| Accountability | P-ACCT-1, P-SIGN-1 | Core recognizes the paper's self-contained pairs of conflicting signed messages and asks the network layer to quarantine the signer. Certificate transcripts count because validation authenticates each attributed message. DA shares are checked individually only when two distinct headers could form a proof. Junk-entry challenges, omission, slashing, and reputation are outside consensus. |
| Theorem 4, exact extension threshold | P-TIPS-1 | The implementation uses the Section 4 threshold `n - f`. The theorem shows that threshold is optimal at `n = 5f + 1` and allows a lower safe threshold for larger committees. The generalized threshold is not implemented. |
| Section 6, backup proposals | P-VIEW-1, P-LEAD-1 | Not implemented. Each view has one scheduled leader proposal, and timeout and nullification handle a crashed leader. |
| Section 6, skipping leaders that appear crashed | P-VIEW-1, P-LIVE-1 | Implemented through `Tuning::skip_timeout`, which defaults to five view timeouts. On entering a view, a validator times out a remote leader early if it has received nothing from that leader within the window while a view quorum, counting itself, was active. The local leader is never skipped. |
| Section 6, ordering by reputation | P-CONS-1, P-MSYNC-1 | Not implemented. Marshal keeps the fixed horizontal chain order, with no blame set or per-view shuffle. |
| Section 6, the proposal before the extensions | P-CONS-1, P-MSYNC-1 | Implemented. Marshal's two-pass sweep visits positions up to each proposed tip first and the extensions second, each pass in horizontal order, so a halt among extension votes defers only extension content. |
| Section 6, anchor choice and larger V-QCs | P-TIPS-1, P-VQC-1 | Implemented. A leader may pick another greatest-view V-QC if it disseminates that anchor itself. V-QCs may hold `n - f` to `n` messages, and tip extraction uses every retained designated vote, breaking ties between branches deterministically. |
| Section 6, tip commitments | P-COMMIT-1, P-MATTACH-1 | Implemented. Each leader block commits recursively to the safe-tip history, so marshal can authenticate history openings without replaying old V-QCs. |
| Section 6, one signature scheme | P-DA-1, P-VIEW-1 | Not implemented. DA certificates and nullifications use threshold signatures, and votes and V-QC/L-QC transcripts use aggregate signatures. |
| Section 6, batch verification and transport | P-ADM-1, P-OBS-1, P-ASYNC-1 | Batch verification is implemented, with optimistic aggregate checks that isolate invalid artifacts. Content-addressed vote transport and advertise-and-pull forwarding are not implemented. |

## Fuzz campaigns

| Campaign | Covers | Independent check | Limits |
|---|---|---|---|
| `multimmit_twins` | P-ACCT-1, P-CONS-1 (consensus side), P-TIPS-1, P-LEAD-1, P-LIVE-1, and Lemma 6 | Exact finality reports are joined with the digest-addressed proposals, votes, blocks, and certificates seen before adversarial delivery, and a run completes only with compatible, complete leader and producer ancestry. Each time an honest node blocks a peer, the campaign snapshots the prefix delivered to that node and checks that decision against evidence verified independently from that prefix. | Decoded wire paths matter only once machine-reported finality authenticates their digest. The fixed accountability regression expects the observer to block the signer exactly once, and the real-crypto machine matrix covers every proof family. Each input runs one bounded random topology and schedule, so not every input produces a conflict. |
| `multimmit_machine` and `multimmit_engine_{minpk,minsig}` | P-SIGN-1, P-CUST-1, P-EXP-1, P-REP-1, P-PUB-1, P-BND-1 | The symbolic World checks that replaying a crash prefix reaches the same durable outcome. The real engines interrupt custody recovery and check that nothing is released early. | The World uses symbolic cryptography and storage. The engine campaign checks recovery mechanisms but does not compare against a full uninterrupted run. |
| `multimmit_artifacts` | P-ADM-1, P-ACCT-1 (attribution), P-VQC-1, P-LEAD-1 (certificate validity), and Section 4 V-QC and L-QC validity | Mutation metadata predicts whether each certificate should be accepted, independently of cached and uncached verification. | Scheme-level artifact validation only. It does not run the stance rules of correct processes or runtime admission. |
| `multimmit_algebra` | P-TIPS-1, P-LEAD-1, and Section 4 tip extraction | Incremental extraction is compared with materialized support counts and with batch insertion in reverse order. | It does not build `Emit`, model certifiability, or prove that later orderings are preserved. Histories that are valid for the algebra need not all be reachable Byzantine executions, and the fixture shares protocol constructors with the code under test. |
| `multimmit_marshal` | P-CONS-1 (ordering side), P-MSYNC-1, P-MDEL-1, and Section 4 ordering | Incremental sweep, replay, reconciliation, and frontier state are compared with an explicit `(pass, offset, chain)` enumeration. | One ordering instance and logical recovery state. It does not cover cross-replica consensus, physical storage recovery, or durable application acknowledgements. |
| `multimmit_wire` | P-ADM-1 and message framing from Sections 4.1 and 4.2 | Every network plane is decoded under protocol bounds, and each successful decode must re-encode to the same bytes. | Syntax, bounds, and canonical encoding only. `multimmit_artifacts` covers semantic certificate validity. |

## Keeping this document current

`just check-properties`, which CI runs, fails when a cited test no longer exists. It checks names
only, so whoever cites a test must make sure it runs the mapped production code and asserts the
stated property.
