diff --git a/resolver/src/p2p/fetcher.rs b/resolver/src/p2p/fetcher.rs
index f94bec444..6ef6f6e5a 100644
--- a/resolver/src/p2p/fetcher.rs
+++ b/resolver/src/p2p/fetcher.rs
@@ -1,4 +1,5 @@
 use crate::p2p::wire;
 use commonware_cryptography::PublicKey;
+use commonware_codec::Encode;
 use commonware_p2p::{CheckedSender, utils::codec::WrappedSender, Recipients, Sender};
 use commonware_runtime::{
@@ -13,5 +14,6 @@ use prometheus_client::{
     encoding::EncodeLabelSet,
     metrics::{family::Family, gauge::Gauge, histogram::Histogram},
 };
+use bytes::Bytes;
 use rand::{seq::SliceRandom, Rng};
 use std::{
@@ -305,7 +307,8 @@ where
                     id,
                     payload: wire::Payload::Request(key.clone()),
                 };
-                match checked.send(message, self.priority_requests).await {
+                let encoded = Bytes::from(message.encode());
+                match checked.send(encoded, self.priority_requests).await {
                     Ok(sent) if !sent.is_empty() => {
                         // Success - move from pending to active
                         self.requests_sent.inc(Status::Success);
