syntax = "proto3";
package wire;

message Signature {
    bytes public_key = 1;
    bytes signature = 2;
}

message AckPart {
    Signature signature = 1;
    bytes car_hash = 2;
}

message Car {
    Signature signature = 1;
    uint64 index = 2;
    bytes payload = 3;
    bytes parent = 4;
}